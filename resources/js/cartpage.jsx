    import CartPage from "./CartPage";

    export default function App() {
    return (
        <CartPage
        cart={[]} 
        removeFromCart={() => {}}
        clearCart={() => {}}
        setActivePage={() => {}}
        />
    );
    }
